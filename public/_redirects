# === Marken-/Alias-Domains → Fortress (greift, sobald die Domain als
#     Netlify-Domain-Alias eingetragen ist; Netlify stellt dann auch das
#     SSL-Zertifikat automatisch bereit) ===
https://kofly.at/*                        https://gleitschirm-tandemflug.com/de    301!
https://www.kofly.at/*                    https://gleitschirm-tandemflug.com/de    301!
https://airpark-lienzerdolomiten.com/*    https://gleitschirm-tandemflug.com/de    301!
https://www.airpark-lienzerdolomiten.com/* https://gleitschirm-tandemflug.com/de   301!

# === Alte WordPress-URLs ===
# Pfad-Redirects liegen in proxy.ts (LEGACY_REDIRECTS): die Locale-Middleware
# laeuft auf Netlify als Edge-Function VOR dieser Datei und wuerde die Regeln
# hier nie feuern lassen (308 nach /de/<pfad> -> 404).
# Hier bleiben nur Pfade mit Punkt, die der Middleware-Matcher ausschliesst.
/wp-login.php         /de           301
/xmlrpc.php           /de           301
/wp-cron.php          /de           301
